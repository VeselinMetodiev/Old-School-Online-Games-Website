<!DOCTYPE html>
<html lang="en">
  <%- include('includes/head') %>
  <body>
    <div class="container">
      <%- include('includes/header') %>
      <!-- Main Content Layout -->
      <div class="content-wrapper">
        <div class="left-column">
          <div class="box">
            <div class="box-title">Редактирай игра: <%= game.title %></div>

            <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
            <div class="alert alert-danger">
              <ul>
                <% errors.forEach(error => { %>
                <li><%= error %></li>
                <% }); %>
              </ul>
            </div>
            <% } %>

            <form action="/game/<%= game.id %>/edit" method="POST">
              <div class="form-group">
                <label for="game-title">Заглавие:</label>
                <input
                  type="text"
                  id="game-title"
                  name="title"
                  class="form-control"
                  value="<%= game.title %>"
                  required
                />
              </div>
              <div class="form-group">
                <label for="game-description">Описание:</label>
                <textarea
                  id="game-description"
                  name="description"
                  class="form-control"
                  rows="5"
                  required
                >
<%= game.description %></textarea
                >
              </div>
              <div class="form-group">
                <label for="game-thumbnail">URL на изображението:</label>
                <input
                  type="url"
                  id="game-thumbnail"
                  name="thumbnail_url"
                  class="form-control"
                  value="<%= game.thumbnail_url %>"
                  required
                />
              </div>
              <button type="submit" class="btn btn-primary">
                Запази промените
              </button>
            </form>
          </div>
        </div>

        <div class="right-column">
          <div class="box">
            <div class="box-title">Бележка</div>
            <p>
              Моля, попълнете всички полета, за да актуализирате информацията за
              играта.
            </p>
          </div>
        </div>
      </div>

      <%- include('includes/footer') %>
    </div>
  </body>
</html>
